# tastawe

Пример простого приложения для мониторинга Minecraft серверов (React + Vite + Tailwind).

## Быстрый старт ✅

1. Установите зависимости:

```bash
npm install
```

2. Запустите dev-сервер:

```bash
npm run dev
```

3. Откройте в браузере адрес, который покажет Vite (обычно http://localhost:5173).

## Что внутри 🔧

- React (Vite)
- React Router Dom (v6+)
- Tailwind CSS
- Axios для запросов
- DOMPurify для безопасного рендеринга MOTD HTML

### Компоненты

- `src/pages/Home.jsx` — страница поиска хоста
- `src/pages/ServerStatus.jsx` — страница мониторинга `/ip/:host`
- `src/App.jsx` — маршрутизация

### Примечания

- API: `https://api.mcsrvstat.us/3/{host}`
- Для безопасного рендеринга MOTD используется `dompurify`.

Если хотите, могу добавить авто-обновление статуса (polling) или тесты для компонентов — напишите, что предпочитаете.